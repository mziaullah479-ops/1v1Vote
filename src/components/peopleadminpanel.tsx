import React, { useEffect, useState } from 'react';
import { Archive, CheckCircle, DatabaseBackup, Edit3, Image, LockKeyhole, Plus, RefreshCw, RotateCcw, Save, ShieldCheck, X } from 'lucide-react';
import { Person, PersonCategory, PersonCountry } from '../types';

const categories: PersonCategory[] = ['Public Figure', 'Religious Scholar', 'Politics', 'Creator', 'Sports', 'Entertainment', 'Business'];
const countries: PersonCountry[] = ['Pakistan', 'India', 'USA', 'Global'];
const emptyForm: Partial<Person> = { name: '', shortBio: '', bio: '', category: 'Public Figure', country: 'Global', avatar: '', profileUrl: '', researchUrl: '' };

export const PeopleAdminPanel: React.FC = () => {
  const [authenticated, setAuthenticated] = useState(false);
  const [configured, setConfigured] = useState(true);
  const [password, setPassword] = useState('');
  const [people, setPeople] = useState<Person[]>([]);
  const [form, setForm] = useState<Partial<Person>>(emptyForm);
  const [editingId, setEditingId] = useState('');
  const [message, setMessage] = useState('');
  const [busy, setBusy] = useState(false);

  const loadPeople = async () => {
    const response = await fetch('/api/admin/people', { credentials: 'include', cache: 'no-store' });
    if (!response.ok) return;
    setPeople((await response.json()).people || []);
  };

  useEffect(() => {
    fetch('/api/admin/session', { credentials: 'include' }).then((response) => response.json()).then((data) => { setAuthenticated(Boolean(data.authenticated)); setConfigured(data.configured !== false); if (data.authenticated) void loadPeople(); });
  }, []);

  const login = async (event: React.FormEvent) => {
    event.preventDefault(); setBusy(true); setMessage('');
    const response = await fetch('/api/admin/login', { method: 'POST', credentials: 'include', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ password }) });
    const data = await response.json().catch(() => ({})); setBusy(false);
    if (!response.ok) return setMessage(data.error || 'Unable to sign in.');
    setPassword(''); setAuthenticated(true); await loadPeople();
  };

  const save = async (event: React.FormEvent) => {
    event.preventDefault(); setBusy(true); setMessage('');
    const response = await fetch(editingId ? `/api/admin/people/${encodeURIComponent(editingId)}` : '/api/admin/people', { method: editingId ? 'PUT' : 'POST', credentials: 'include', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) });
    const data = await response.json().catch(() => ({})); setBusy(false);
    if (!response.ok) return setMessage(data.error || 'Could not save this profile.');
    setMessage(editingId ? 'Profile updated safely.' : 'Profile added safely.'); setForm(emptyForm); setEditingId(''); await loadPeople();
  };

  const archive = async (person: Person) => {
    if (!window.confirm(`Archive ${person.name}? Votes and history stay in the database.`)) return;
    await fetch(`/api/admin/people/${encodeURIComponent(person.id)}`, { method: 'DELETE', credentials: 'include' }); await loadPeople(); setMessage(`${person.name} archived. Data was not deleted.`);
  };

  const restore = async (person: Person) => { await fetch(`/api/admin/people/${encodeURIComponent(person.id)}/restore`, { method: 'POST', credentials: 'include' }); await loadPeople(); setMessage(`${person.name} restored.`); };
  const refresh = async () => { setBusy(true); const response = await fetch('/api/admin/people/refresh', { method: 'POST', credentials: 'include' }); const data = await response.json().catch(() => ({})); setBusy(false); setMessage(response.ok ? `Research complete: ${data.automation?.lastPublished || 0} new profiles published.` : data.error || 'Research failed.'); await loadPeople(); };
  const backup = async () => { const response = await fetch('/api/admin/backup', { method: 'POST', credentials: 'include' }); const data = await response.json().catch(() => ({})); setMessage(response.ok ? `Backup created: ${data.backup}` : data.error || 'Backup failed.'); };
  const setField = (key: keyof Person, value: string) => setForm((current) => ({ ...current, [key]: value }));

  if (!authenticated) return <main className="flex min-h-screen items-center justify-center bg-[#060a13] px-4"><form onSubmit={(event) => void login(event)} className="w-full max-w-md rounded-3xl border border-slate-800 bg-[#0b1221] p-7 shadow-2xl"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl border border-amber-500/40 bg-amber-500/10 text-amber-300"><LockKeyhole className="h-7 w-7" /></div><h1 className="mt-5 text-center text-2xl font-black text-white">Secure profile admin</h1><p className="mt-2 text-center text-sm text-slate-400">Edit, add, archive, restore, and back up public profiles.</p>{!configured && <p className="mt-5 rounded-xl border border-rose-500/40 bg-rose-950/30 p-3 text-xs text-rose-300">Admin access is not configured on this deployment.</p>}{message && <p className="mt-5 rounded-xl border border-rose-500/40 bg-rose-950/30 p-3 text-xs text-rose-300">{message}</p>}<input autoFocus type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Admin password" className="mt-5 w-full rounded-xl border border-slate-700 bg-[#060b17] px-4 py-3 text-white outline-none focus:border-amber-400" disabled={!configured} /><button disabled={!configured || !password || busy} className="mt-4 w-full rounded-xl bg-amber-600 py-3 font-bold text-white disabled:opacity-40">{busy ? 'Checking...' : 'Unlock admin panel'}</button><a href="/" className="mt-4 block text-center text-sm text-slate-400 hover:text-white">Return to public site</a></form></main>;

  return <main className="min-h-screen bg-[#060a13] px-4 py-7 text-slate-100 sm:px-6"><div className="mx-auto max-w-7xl"><header className="flex flex-wrap items-center justify-between gap-4"><div><div className="flex items-center gap-2 text-xs font-black uppercase tracking-[0.2em] text-emerald-300"><ShieldCheck className="h-4 w-4" /> Secure admin session</div><h1 className="mt-2 text-3xl font-black text-white">Profile control room</h1><p className="mt-2 text-sm text-slate-400">Turso is authoritative in production. Archive keeps votes and history safe.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => void refresh()} disabled={busy} className="inline-flex items-center gap-2 rounded-xl border border-sky-500/40 bg-sky-500/10 px-3 py-2 text-xs font-bold text-sky-200"><RefreshCw className="h-4 w-4" /> Research now</button><button onClick={() => void backup()} className="inline-flex items-center gap-2 rounded-xl border border-emerald-500/40 bg-emerald-500/10 px-3 py-2 text-xs font-bold text-emerald-200"><DatabaseBackup className="h-4 w-4" /> Backup now</button><button onClick={() => void fetch('/api/admin/logout', { method: 'POST', credentials: 'include' }).then(() => window.location.href = '/')} className="rounded-xl border border-slate-700 px-3 py-2 text-xs font-bold text-slate-300">Sign out</button></div></header>{message && <div className="mt-5 flex items-center gap-2 rounded-2xl border border-emerald-500/30 bg-emerald-950/30 p-3 text-xs font-bold text-emerald-200"><CheckCircle className="h-4 w-4" />{message}</div>}<div className="mt-7 grid gap-6 lg:grid-cols-[390px_1fr]"><form onSubmit={(event) => void save(event)} className="h-fit rounded-3xl border border-slate-800 bg-[#0b1221] p-5"><div className="flex items-center justify-between"><h2 className="text-lg font-black text-white">{editingId ? 'Edit profile' : 'Add profile'}</h2>{editingId && <button type="button" onClick={() => { setEditingId(''); setForm(emptyForm); }} className="text-slate-500 hover:text-white"><X className="h-4 w-4" /></button>}</div><div className="mt-4 space-y-3">{([['name','Full name'],['shortBio','Short bio'],['avatar','Real image HTTPS URL'],['profileUrl','Public source URL'],['researchUrl','Research URL']] as Array<[keyof Person,string]>).map(([key,label]) => <label key={String(key)} className="block text-xs font-bold text-slate-400">{label}<input required={key === 'name' || key === 'avatar'} value={String(form[key] || '')} onChange={(event) => setField(key, event.target.value)} className="mt-1 w-full rounded-xl border border-slate-800 bg-[#060b17] px-3 py-2.5 text-sm text-white outline-none focus:border-sky-500" /></label>)}<label className="block text-xs font-bold text-slate-400">Detailed bio<textarea value={String(form.bio || '')} onChange={(event) => setField('bio', event.target.value)} className="mt-1 min-h-28 w-full rounded-xl border border-slate-800 bg-[#060b17] px-3 py-2.5 text-sm text-white outline-none focus:border-sky-500" /></label><div className="grid grid-cols-2 gap-2"><label className="text-xs font-bold text-slate-400">Category<select value={form.category || 'Public Figure'} onChange={(event) => setField('category', event.target.value)} className="mt-1 w-full rounded-xl border border-slate-800 bg-[#060b17] px-2 py-2.5 text-sm text-white">{categories.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-xs font-bold text-slate-400">Country<select value={form.country || 'Global'} onChange={(event) => setField('country', event.target.value)} className="mt-1 w-full rounded-xl border border-slate-800 bg-[#060b17] px-2 py-2.5 text-sm text-white">{countries.map((item) => <option key={item}>{item}</option>)}</select></label></div></div><button disabled={busy} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-sky-500 px-3 py-3 text-sm font-black text-slate-950 disabled:opacity-50">{editingId ? <Save className="h-4 w-4" /> : <Plus className="h-4 w-4" />}{editingId ? 'Save profile' : 'Add profile'}</button></form><section className="rounded-3xl border border-slate-800 bg-[#0b1221] p-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-black text-white">All stored profiles</h2><p className="mt-1 text-xs text-slate-500">{people.filter((person) => !person.archivedAt).length} public · {people.filter((person) => person.archivedAt).length} archived</p></div><Archive className="h-5 w-5 text-amber-300" /></div><div className="mt-4 space-y-2">{people.map((person) => <div key={person.id} className={`flex flex-wrap items-center gap-3 rounded-2xl border p-3 ${person.archivedAt ? 'border-slate-800/60 bg-slate-950/30 opacity-60' : 'border-slate-800 bg-[#060b17]'}`}><img src={person.avatar} alt="" width={44} height={44} className="h-11 w-11 rounded-xl object-cover" /><div className="min-w-0 flex-1"><div className="truncate text-sm font-black text-white">{person.name}</div><div className="mt-1 text-[10px] text-slate-500">{person.category} · {person.country} · {person.archivedAt ? 'Archived' : 'Public'}</div></div><button onClick={() => { setEditingId(person.id); setForm(person); window.scrollTo({ top: 0, behavior: 'smooth' }); }} className="inline-flex items-center gap-1 rounded-lg border border-slate-700 px-2 py-2 text-[10px] font-bold text-slate-300"><Edit3 className="h-3.5 w-3.5" /> Edit</button>{person.archivedAt ? <button onClick={() => void restore(person)} className="inline-flex items-center gap-1 rounded-lg border border-emerald-500/40 px-2 py-2 text-[10px] font-bold text-emerald-300"><RotateCcw className="h-3.5 w-3.5" /> Restore</button> : <button onClick={() => void archive(person)} className="inline-flex items-center gap-1 rounded-lg border border-rose-500/40 px-2 py-2 text-[10px] font-bold text-rose-300"><Archive className="h-3.5 w-3.5" /> Archive</button>}</div>)}</div></section></div></div></main>;
};
